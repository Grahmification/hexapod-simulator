namespace Hexapod_Simulator.Helix.ViewModels
{
    /// <summary>
    /// May want to add 6 of these to the platform class so each translation slider can be bound to one of these. Then the VM will handle the max/min limits, default value, etc, for translation
    /// </summary>
    public class TranslationVM
    {
    }
}
